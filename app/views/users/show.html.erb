<p id="notice"><%= notice %></p>

<p>user.show </p>
<p>
  <strong> 
  <%= @user.username %>
  </strong>
</p>

<p> has made <%= pluralize(@user.ratings.count,'rating') %>, 
Average rating: <%= @user.ratings.average_rating %> 

</p>


<ul>
  <% @user.ratings.each do |rating| %>

      <li> <%= rating %> <%= link_to 'delete', rating, :method => :delete, data: { confirm: 'Are you sure?' } %>

 </li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
